import styled from 'styled-components'

const Avatar = styled.img.attrs({ 'data-testid': 'avatar' })`
  height: 170px;
  width: 170px;
  background: lightgray;
  border: 1px solid gray;
  border-radius: 50%;
`

export default Avatar
